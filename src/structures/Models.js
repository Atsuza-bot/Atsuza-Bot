/*const { Shema, model } = require('mongoose');

const guildShema = Shema({
    id: String,
    prefix: {
        type: String,
        default: ';'
    }
})

module.exports = {
    Guild: model('Guild', guildShema)
}*/